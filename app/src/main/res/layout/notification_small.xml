<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="15dp"
    android:paddingVertical="10dp">

    <TextView
        android:id="@+id/notification_eng"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/notification_app_icon"
        android:textColor="@color/material_light_primary_text"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/notification_kor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/notification_eng"
        android:layout_toRightOf="@id/notification_app_icon"
        android:textColor="@color/material_light_primary_text"
        android:textSize="14sp" />

    <Button
        android:id="@+id/notification_show_vocabulary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@android:drawable/screen_background_light_transparent"
        android:clickable="true"
        android:text="다른 단어" />

    <ImageView
        android:id="@+id/notification_app_icon"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="15dp"
        app:srcCompat="@drawable/ic_image" />
</RelativeLayout>